# Better LS
alias ls='ls -aFG'

# Colors
alias grep='grep --color=auto'
alias less='less -R'

# Use nvim
alias v='nvim'
alias vi='nvim'
alias vim='nvim'

# Nix
alias nix-shell='nix-shell --command fish'

